<div class="widget instagram{{#unless widget_title}} no-title{{/unless}}{{#if custom_class}} {{custom_class}}{{/if}}">
	{{#if widget_title}}
	<h3 class="widget-title">{{{widget_title}}}</h3>
	{{/if}}
	<div class="widget-content">
		<ul class="instagram-feed" data-userid="brendazambranos" data-api-token="{{api_token}}" data-api-clientid="40fd7b3a08bb4130be30bb30e1c3bad7" data-mode="gallery" data-display="{{display}}" data-cols="{{cols}}" data-popup-preview="{{popup_preview}}">
		</ul>
		{{#if show_follow_box}}
		<a class="follow-box instagram" target="_blank" href="https://instagram.com/brendazambranos">
			{{#if follow_box_text}}
				{{follow_box_text}}
			{{else}}
				Más contenido en Instagram			
			{{/if}}
		</a>
		{{/if}}
	</div>
</div>